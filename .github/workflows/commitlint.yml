on: [pull_request]

jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: mfocko/actions@conventional-commits
        with:
          from: ${{ github.event.base.sha }}
          to: ${{ github.event.head.sha }}
